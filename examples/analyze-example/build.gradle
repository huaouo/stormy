dependencies {
    compileOnly project(':stormy-api')
}

shadowJar {
    archiveFileName.set("analyze-example.jar")

    manifest {
        attributes(
                'Main-Class': 'com.huaouo.stormy.example.AnalyzeTopology'
        )
    }
}
