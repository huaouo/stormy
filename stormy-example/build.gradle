dependencies {
    compileOnly project(':stormy-api')
}

shadowJar {
    archiveFileName.set("example.jar")

    manifest {
        attributes(
                'Main-Class': 'com.huaouo.stormy.example.TopologyImpl'
        )
    }
}
